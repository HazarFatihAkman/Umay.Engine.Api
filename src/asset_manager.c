#include "../include/asset_manager.h"

#include <stdint.h>
#include <unistd.h>

static const asset_route_t *asset_route;

void init_asset_manager(asset_route_t asset_routes[]) {
  asset_routes = asset_routes;
}

char *find_asset_folder_by_flag(uint32_t flag) {
  return NULL;
}

